{
  "name": "@sila/core",
  "private": true,
  "version": "1.0.0",
  "description": "Core for Sila that can be used on clients, servers, and inside other services",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "watch": "tsc -p tsconfig.json --noEmit --watch",
    "test": "vitest run --reporter=verbose --no-watch",
    "test:node": "vitest run --config tests/vitest.config.ts",
    "test:svelte": "vitest run --config tests/vitest.config.svelte.ts"
  },
  "dependencies": {
    "aiwrapper": "^3.0.0-beta.1",
    "reptree": "^0.9.0",
    "defuddle": "github:mitkury/defuddle#cd8c7f1d39dbd4800918472b880f96724c0a2441"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "~5.8.3",
    "vitest": "^2.1.1",
    "tsx": "^4.0.0",
    "jsdom": "^24.0.0",
    "@testing-library/svelte": "^4.0.0",
    "@sveltejs/vite-plugin-svelte": "^6.0.0"
  }
}
