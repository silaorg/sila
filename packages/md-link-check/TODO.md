# TODO: publish as a standalone npm package

- Remove "private": true from package.json.
- Pick a public package name.
- Add repository and bugs fields in package.json.
- Add homepage in package.json.
- Add keywords in package.json.
- Add a changelog.
- Decide on license and confirm SPDX identifier.
- Add a build step if we want TypeScript.
- Add tests and a CI workflow.
- Add a release process and versioning policy.
- Add usage docs for install and CLI usage.
